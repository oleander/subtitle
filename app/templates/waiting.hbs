<div id="info">
  Drop Here
  <div>
    {{#if isChangingLang}}
      {{#change-lang action="setLanguage" selected=currentLangID}}
        <option value="eng">English</option>
        <option value="swe">Swedish</option>
      {{/change-lang}}
    {{else}}
      <i>
        <strong>
          <a href="#" {{action "changeLang"}}>{{currentLang}}</a>
        </strong>
      </i>
    {{/if}}
  </div>
</div>